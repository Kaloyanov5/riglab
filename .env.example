# Copy this file to .env and adjust the values before running `docker compose up`.
# Compose reads .env automatically; .env itself is gitignored (never commit real secrets).

# MySQL root password (also used by the app to connect to the db service)
DB_PASSWORD=root

# Initial admin account created on first startup
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
